% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{dyn_phase_lock_cpp}
\alias{dyn_phase_lock_cpp}
\title{Instantaneous phase-locking matrices (C++ backend)}
\usage{
dyn_phase_lock_cpp(phases)
}
\arguments{
\item{phases}{NumericMatrix [N x Tmax]. Instantaneous phases in radians,
as returned by \code{\link[=hilbert_phases]{hilbert_phases()}}.}
}
\value{
A NumericVector with a \code{dim} attribute [N, N, n_T] (i.e. a
3-D array), where \code{n_T = Tmax - 20}.
}
\description{
Internal workhorse called by \code{\link[=dyn_phase_lock]{dyn_phase_lock()}}. Iterates over the trimmed
timepoints (dropping the first and last 10) and fills an [N, N, n_T]
array with \eqn{\cos(\phi_i - \phi_j)} for every parcel pair.
}
\details{
Exploits the symmetry \eqn{\cos(a-b) = \cos(b-a)}: only the upper triangle
is computed via \code{std::cos}; the diagonal is set to 1 directly, and
values are mirrored to the lower triangle. This halves the number of
trigonometric evaluations relative to a naive double loop.
}
\keyword{internal}
