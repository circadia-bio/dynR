% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_subject.R
\name{stack_leida}
\alias{stack_leida}
\title{Stack LEiDA eigenvectors across subjects}
\usage{
stack_leida(batch_result, add_subject_id = TRUE)
}
\arguments{
\item{batch_result}{Named list of \code{dynR_leida} objects, as returned by
\code{batch_leida()}.}

\item{add_subject_id}{Logical. Prepend a \code{subject} column with the subject
name? Default \code{TRUE}.}
}
\value{
If \code{add_subject_id = TRUE}: a data frame with columns \code{subject} and
one column per parcel (named \code{V1}, \code{V2}, ...).
If \code{FALSE}: a plain numeric matrix [total_timepoints x N].
}
\description{
Combine the LEiDA eigenvector matrices from a list of \code{dynR_leida} results
into a single matrix (or data frame) ready for cross-subject K-means
clustering.
}
\examples{
set.seed(3)
ts_list <- list(
  sub01 = matrix(rnorm(10 * 200), nrow = 10),
  sub02 = matrix(rnorm(10 * 200), nrow = 10)
)
results <- batch_leida(ts_list, filter = FALSE)
leida_all <- stack_leida(results)
dim(leida_all)
}
\seealso{
\code{batch_leida()}
}
