% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_subject.R
\name{batch_sw}
\alias{batch_sw}
\title{Batch sliding-window pipeline across subjects}
\usage{
batch_sw(data, window, ...)
}
\arguments{
\item{data}{Named list of [N x Tmax] matrices, or a 3-D array
[N, Tmax, subjects]. See \code{batch_leida()}.}

\item{window}{Integer. Window size in timepoints passed to \code{sw_pipeline()}.}

\item{...}{Additional arguments passed to \code{sw_pipeline()}.}
}
\value{
Named list of \code{dynR_sw} objects, one per subject.
}
\description{
Apply \code{sw_pipeline()} to multiple subjects in one call. Accepts either a
named list of [N x Tmax] matrices or a 3-D array [N x Tmax x subjects].
}
\examples{
set.seed(2)
ts_list <- list(
  sub01 = matrix(rnorm(10 * 200), nrow = 10),
  sub02 = matrix(rnorm(10 * 200), nrow = 10)
)
results <- batch_sw(ts_list, window = 20, filter = FALSE)
names(results)
}
\seealso{
\code{batch_leida()}, \code{sw_pipeline()}
}
