% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transitions.R
\name{dyn_transitions}
\alias{dyn_transitions}
\title{State transition probabilities (Markov analysis)}
\usage{
dyn_transitions(tbl, vars, cVar, sortBy, groupBy, remIntra = FALSE)
}
\arguments{
\item{tbl}{A data frame in long format (one row per timepoint).}

\item{vars}{Character vector of grouping / covariate column names to
preserve in the output (e.g. \code{c("sub", "ses", "age", "sex")}).}

\item{cVar}{Character. Name of the column holding integer cluster labels.}

\item{sortBy}{Character vector of column names used to sort rows within each
group before transitions are computed (typically \code{c("sub", "ses", "ttime")}).}

\item{groupBy}{Character vector of column names that define independent
sequences (typically \code{c("sub", "ses")}). Transitions are never computed
across group boundaries.}

\item{remIntra}{Logical. If \code{TRUE}, self-transitions (state → same state)
are removed before normalising probabilities. Default \code{FALSE}.}
}
\value{
A nested tibble with columns \code{tag}, \code{source}, \code{target}, and \code{data}.
\code{tag} encodes the transition as \code{"<source>_<target>"}. Each \code{data}
element is a per-group tibble with columns inherited from \code{vars} plus:
\item{n}{Raw transition count.}
\item{tot}{Total transitions out of \code{source} for that group.}
\item{nCount}{Transition probability (\code{n / tot}).}
}
\description{
From a long-format data frame of cluster labels ordered in time, compute
first-order Markov transition probabilities between brain states: for each
source state, the fraction of transitions that lead to each target state.
}
\details{
Ported from \code{clusters_markov()} in the \code{neonatal_dfc} analysis pipeline
(França et al., \emph{Nat Commun}).
}
\examples{
set.seed(1)
df <- data.frame(
  sub   = rep(c("A", "B"), each = 50),
  ses   = 1L,
  ttime = rep(seq_len(50), 2),
  clus4 = sample(1:4, 100, replace = TRUE)
)
tr <- dyn_transitions(
  df,
  vars    = c("sub", "ses"),
  cVar    = "clus4",
  sortBy  = c("sub", "ses", "ttime"),
  groupBy = c("sub", "ses")
)
tr
}
