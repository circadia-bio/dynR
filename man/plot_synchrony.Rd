% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dynR.R
\name{plot_synchrony}
\alias{plot_synchrony}
\title{Plot Kuramoto synchrony time series}
\usage{
plot_synchrony(synchrony, title = "Kuramoto order parameter")
}
\arguments{
\item{synchrony}{Numeric vector. Kuramoto R(t), as returned by \code{kuramoto()}
or the \verb{$synchrony} element of \code{leida_pipeline()}.}

\item{title}{Character. Plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Line plot of the Kuramoto order parameter R(t) over time, with a dashed
horizontal line at the mean. Uses the dynR periwinkle/brick-red palette.
}
\examples{
set.seed(1)
ts <- matrix(rnorm(10 * 200), nrow = 10)
ph  <- hilbert_phases(ts)
kop <- kuramoto(ph)
plot_synchrony(kop$synchrony)
}
