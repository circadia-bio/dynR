% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kuramoto.R
\name{kuramoto}
\alias{kuramoto}
\title{Kuramoto order parameter and metastability}
\usage{
kuramoto(phases, base = 2, n_bits = 8L)
}
\arguments{
\item{phases}{Numeric matrix [N × Tmax]. Instantaneous phases in radians,
as returned by \code{\link[=hilbert_phases]{hilbert_phases()}}.}

\item{base}{Numeric. Logarithm base for Shannon entropy. Default is 2.}

\item{n_bits}{Integer or \code{NULL}. Bit depth for discretising the synchrony
series before entropy estimation. Default is 8.}
}
\value{
A list with:
\item{metastability}{Numeric scalar. Standard deviation of the Kuramoto
order parameter.}
\item{synchrony}{Numeric vector [Tmax-20]. Kuramoto order parameter
at each (trimmed) timepoint.}
\item{entropy}{Numeric scalar. Shannon entropy of the synchrony series.}
}
\description{
Compute the global Kuramoto order parameter time series, the metastability
index (standard deviation of the order parameter), and Shannon entropy of
synchrony from parcel-level instantaneous phase time series.
The first and last 10 timepoints are discarded (matching \code{\link[=dyn_phase_lock]{dyn_phase_lock()}}).
}
\examples{
set.seed(1)
ts <- matrix(rnorm(10 * 200), nrow = 10, ncol = 200)
phases <- hilbert_phases(ts)
res <- kuramoto(phases)
res$metastability
}
