% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandpass_filter.R
\name{bandpass_filter}
\alias{bandpass_filter}
\title{Butterworth bandpass filter}
\usage{
bandpass_filter(x, flp, fhi, delt, order = 2)
}
\arguments{
\item{x}{Numeric vector. Signal to be filtered.}

\item{flp}{Numeric. Low-pass cutoff frequency (Hz).}

\item{fhi}{Numeric. High-pass cutoff frequency (Hz).}

\item{delt}{Numeric. Sampling interval in seconds (i.e. TR for fMRI).}

\item{order}{Integer. Filter order. Default is 2.}
}
\value{
Numeric vector. Zero-phase filtered signal, same length as \code{x}.
}
\description{
Design and apply a zero-phase Butterworth bandpass filter to a signal,
using scipy-compatible steady-state initial conditions (equivalent to
\code{scipy.signal.sosfiltfilt} / \code{scipy.signal.filtfilt} with
\code{padtype = "odd"}).
}
\details{
Two internal code paths are used depending on \code{order}:
\itemize{
\item \strong{\code{order <= 3}} (typical fMRI use): direct-form b/a coefficients with
the companion-matrix initial-condition method. scipy \code{filtfilt}-parity
validated to \verb{< 1e-9} for standard fMRI parameters.
\item \strong{\code{order >= 4}}: second-order sections (SOS). The companion matrix for
the full-order b/a representation becomes ill-conditioned for high-order
filters with poles close to z = 1 (very low lower cutoff). SOS avoids
this by decomposing the filter into biquad sections, each with a 2x2
companion matrix that is always well-conditioned.
}

Filter coefficients are computed by \code{.butter_bandpass_zpk()}, a bespoke
port of \code{scipy.signal.butter()} that requires no external packages.
}
\examples{
set.seed(1)
x <- rnorm(200)
x_filt <- bandpass_filter(x, flp = 0.01, fhi = 0.1, delt = 2, order = 2)
}
