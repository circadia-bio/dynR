% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandpass_filter.R
\name{bandpass_filter}
\alias{bandpass_filter}
\title{Butterworth bandpass filter}
\usage{
bandpass_filter(x, flp, fhi, delt, order = 2)
}
\arguments{
\item{x}{Numeric vector. Signal to be filtered.}

\item{flp}{Numeric. Low-pass cutoff frequency (Hz).}

\item{fhi}{Numeric. High-pass cutoff frequency (Hz).}

\item{delt}{Numeric. Sampling interval in seconds (i.e. TR for fMRI).}

\item{order}{Integer. Filter order. Default is 2.}
}
\value{
Numeric vector. Zero-phase filtered signal, same length as \code{x}.
}
\description{
Design and apply a zero-phase Butterworth bandpass filter to a signal.
Wraps \code{\link[gsignal:butter]{gsignal::butter()}} and \code{\link[gsignal:filtfilt]{gsignal::filtfilt()}} for convenience.
}
\examples{
set.seed(1)
x <- rnorm(200)
x_filt <- bandpass_filter(x, flp = 0.01, fhi = 0.1, delt = 2, order = 2)
}
