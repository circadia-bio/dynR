% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{get_leida_cpp}
\alias{get_leida_cpp}
\title{Leading eigenvector decomposition (LEiDA) -- C++ / LAPACK backend}
\usage{
get_leida_cpp(sync_conn)
}
\arguments{
\item{sync_conn}{NumericVector with \code{dim} attribute [N, N, t_points].}
}
\value{
NumericMatrix [t_points x N]. Leading eigenvectors, one per row.
}
\description{
Internal workhorse called by \code{\link[=get_leida]{get_leida()}}. For each phase-locking matrix
slice in \code{sync_conn}, calls LAPACK \code{dsyev} (symmetric eigendecomposition)
and returns only the leading eigenvector.
}
\details{
Differences from the R implementation:
\itemize{
\item \code{dsyev} returns eigenvalues in \strong{ascending} order, so the leading
eigenvector is the \strong{last} column of the output (opposite of R's
\code{eigen()} which is descending).
\item The LAPACK workspace is allocated once and reused across all timepoints.
\item Sign convention preserved: row sum forced to be non-positive.
}
}
\keyword{internal}
