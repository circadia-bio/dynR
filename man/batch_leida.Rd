% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_subject.R
\name{batch_leida}
\alias{batch_leida}
\title{Batch LEiDA pipeline across subjects}
\usage{
batch_leida(data, ...)
}
\arguments{
\item{data}{Named list of [N x Tmax] numeric matrices, \strong{or} a 3-D
numeric array with dimensions [N, Tmax, subjects]. For arrays, subject
names are taken from \code{dimnames(data)[[3]]} if set, otherwise \code{"sub1"},
\code{"sub2"}, etc.}

\item{...}{Additional arguments passed to \code{leida_pipeline()} (e.g. \code{flp},
\code{fhi}, \code{delt}, \code{filter}).}
}
\value{
Named list of \code{dynR_leida} objects, one per subject.
}
\description{
Apply \code{leida_pipeline()} to multiple subjects in one call. Accepts either a
named list of [N x Tmax] matrices or a 3-D array [N x Tmax x subjects].
}
\examples{
set.seed(1)
ts_list <- list(
  sub01 = matrix(rnorm(10 * 200), nrow = 10),
  sub02 = matrix(rnorm(10 * 200), nrow = 10)
)
results <- batch_leida(ts_list, filter = FALSE)
names(results)
}
\seealso{
\code{stack_leida()}, \code{leida_pipeline()}
}
