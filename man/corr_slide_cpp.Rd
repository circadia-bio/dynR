% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{corr_slide_cpp}
\alias{corr_slide_cpp}
\title{Sliding-window Pearson correlation (C++ backend)}
\usage{
corr_slide_cpp(timeseries, window, step)
}
\arguments{
\item{timeseries}{NumericMatrix [N x Tmax].}

\item{window}{Integer window size in timepoints.}

\item{step}{Integer step between window onsets.}
}
\value{
List with:
\item{corr_mats}{NumericVector with dim [N, N, n_windows].}
\item{idx}{IntegerVector of 1-indexed window onset positions.}
}
\description{
Internal workhorse called by \code{\link[=corr_slide]{corr_slide()}}. Computes Pearson correlation
matrices directly on the input matrix without allocating per-window
submatrices or calling R's \code{cor()}.
}
\details{
Loop ordering is chosen for cache efficiency on the column-major R matrix:
the innermost loop over \code{t} (timepoints) within a window iterates over
consecutive memory locations in each column of \code{timeseries}, so the
cross-product accumulation step is sequential in memory.
}
\keyword{internal}
