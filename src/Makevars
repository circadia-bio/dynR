# Link LAPACK for dsyev_ (get_leida.cpp).
#
# dsyev_ is called via F77_CALL(dsyev) declared in R_ext/Lapack.h.
# That symbol resolves at load time through R's own bundled libRlapack
# (Linux) or the Accelerate framework (macOS) without any additional
# -l flags. FLIBS is emptied to prevent R's default Fortran toolchain
# search paths from being injected when gfortran is not installed.
FLIBS =
PKG_LIBS =
