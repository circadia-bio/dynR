# Windows: dsyev_ requires explicit LAPACK linking via Rtools.
# $(LAPACK_LIBS) and $(BLAS_LIBS) expand to the correct Rtools44 libraries;
# $(FLIBS) is included because the Rtools44 gfortran toolchain is present.
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
